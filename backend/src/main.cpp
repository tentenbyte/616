#include <iostream>
#include <string>

int main(int argc, char* argv[]) {
    std::cout << "Backend server starting..." << std::endl;
    
    // 基础服务器逻辑
    std::cout << "Server is running on port 8080" << std::endl;
    
    return 0;
}